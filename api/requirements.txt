# Versions verified against PyPI rather than recalled.

# Agent — Strands Agents SDK, per Plan §09.
# strands-agents-tools is deliberately absent: nothing imports it, and it pulls
# sympy, Pillow, aiohttp, watchdog and slack-bolt into a maternal-health API.
strands-agents==1.54.0

# API
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.3.0   # EmailStr in app/schemas.py

# Data
sqlalchemy==2.0.52
alembic==1.19.1
psycopg[binary]==3.3.5

# Auth
PyJWT==2.13.0
bcrypt==5.0.0
python-dotenv==1.2.3

# Dev
pytest==9.1.1
httpx2==2.12.0   # starlette's TestClient wants httpx2 now
